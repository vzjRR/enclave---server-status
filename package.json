{
  "name": "enclave-server-status",
  "version": "1.0.0",
  "description": "Enclave RP Discord bot that watches the FiveM server and posts bilingual (EN/AR) status alerts",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "deploy": "node src/deploy-commands.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/fivem.js && node --check src/embeds.js && node --check src/statusWatcher.js && node --check src/deploy-commands.js && node --check src/commands/scheduledRestart.js && node --check src/commands/status.js"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  }
}
